unix

쉘의 차이

python

Ubuntu 깔아라

C:\Users\me>c:
C:\Users\me>cd\
C:\mkdir workspace
C:\>cd workspace
C:\workspace>


ls : 리스트 보여줌
cd : change direction 경로 설정
cd.. : 설정한 경로 나가기
pwd : Print Working Directory 작업하고 있는 폴더의 경로를 출력
mkdir : makd directory 디렉토리를 만듦
rmdir : remove directory 디렉토리 지움
echo : 문자열이나 객체를 화면에 출력해줌
ls -al : 숨겨진 파일까지 다 보여줌
rm : 파일 싹 다 삭제

깃허브에 처음 올리는 과정
git init
echo "# 2026_AI_Program" >> README.md
git add README.md
git commit -m "2026.03.18 coding"
git branch -M main
git remote add origin https://github.com/SJH-0923/2026_AI_Program.git
git push -u origin main


깃허브에 올리는 순서
git add . : 폴더 안에 있는 모든 파일들을 담는다.
git add . 파일명: 파일 하나만 담는다.
git commit -m "메시지" : commit을 해야 포장이 된 거임. 따옴표 안에 글이 무조건 있어야 함.
git push origin main

git push origin main : 깃허브에 올리기
git pull origin main : 깃허브에서 불러오기

결과가 없는 함수 : print
결과가 있는 함수 : input



<제어문>
조건문 : if 문, if - else 문
반복문 : for 문, while 문
반복문 흐름 변경 : break 문, continue

-조건문
if : 조건 적용
else if (elif) : else 중에서 추가적인 조건 적용
else : if, elif 말고 다

-반복문
for i in range() : range 안에서 i를 받아와라.
range()에서 괄호 안의 순서 (start, stop, step) 시작, 멈춤, 간격
※ i는 단순 매개변수라서 다른 기호를 사용해도 상관 없다.

-리스트 변수
score = [87,84, 95, 67]
for i in score(리스트 변수)를 넣으면 리스트를 프린트 해준다. 숫자 및 문자로도 받을 수 있다.
list(range(5)) : range(5)를 list로 바꾼다.

myString = "apple"
for ch in myString :
    print(ch) ---> 이렇게 입력하면 문자 스펠링 하나하나가 따로따로 찍힌다.

리스트가 -1이면 리스트의 제일 마지막 요소를 프린트 함. (한 바퀴만 돌 수 있다.)

list.append('') : list라는 리스트 안에 새로운 요소를 추가한다.

!!! 리스트 매서드 !!!
메소드 : 객체가 가지고 있는 명령어
list.메소드 : 리스트는 정보도 담고 있지만 행동(추가, 삭제 등)이 가능 ex) .append() 메소드 등
.append() : 요소 추가
.pop() : 요소 삭제 (괄호 안에 숫자 있으면 순서, 없으면 맨 마지막 요소 삭제)
.remove : 요소 삭제(괄호 안에 요소를 넣어서 삭제)


min() : 
max() : 
sum() : 
reverse() : sort reverse = True 랑 똑같음

list[1:5] : 1요소부터 5요소까지 슬라이싱. (앞에서부터 start, stop, step 순으로 똑같다.)

줄바꿈 기호 \n
기본적으로 print()는 print( , end = '\n')가 있는데 end = '\n'는 생략이 되어 있다.
그래서 가로로 배열하고자 할 때는 end = ' '로 \n을 지워주면 됨

dictionary
key와 value가 존재
형태 : 딕셔너리 이름 = {key : value, ...}
person = {'이름' : '홍길동', '나이' : 26, '몸무게' : 82 }
위와 같이 했을 때, 이름(key)과 홍길동(value)가 있다
print(person[key값])을 하면 해당 value를 준다

dictionary에 요소 추가 방법
형태 : 딕셔너리 이름[key] = value
person['국적'] = '대한민국'

dictionary에 요소 삭제 방법
형태 : del 딕셔너리 이름['key']
del person['국적']

딕셔너리 부등호 지원 안 함.

!!! 딕셔너리 메서드 !!!

(파이썬 모든 변수는 객체변수)

튜플
튜플과 리스트의 차이 : 튜플은 정의하면 추가 및 삭제가 안 된다.
함수의 아웃풋은 튜플
output_list = list(output) 아웃풋 값을 리스트로 바꾸는 방법

튜플의 +, * 연산도 리스트와 같다.
ex) 같은 튜플을 더하면 값이 더해지는 게 아니라 같은 튜플이 두 개

!!! 튜플 매서드 !!!

집합
집합명 = {요소들} ---> 집합은 중괄호 사용

!!! 집합 매서드 !!!

json 파일
import json
json.dump : 파일을 저장 및 생성
json.dumps : 파일을 생성하지는 않고 문자열로 변환하여 변수에 담음.

기존에 만들었던 주소록을 유지하고 거기서 추가추가 하는 방식의 코딩을 짜라.
그거 말고도 주소록 저장을 위한 편리한 기능 넣어서 코딩을 짜라.
기존 주소록을 삭제하는 기능
주소록을 탐색하는 기능 등등...

모듈
다른 사람이 만들어 놓은 거 쓰는 거
매서드는 class에 속한 함수
[모듈 이름].[클래스 이름].[메소드이름]() ---> datetime.datetime.now.()
import ~ as 별칭

dir 함수 : 모듈이 가진 클래스의 목록을 출력
replace() 메소드 : datetime에서 날짜나 시간 값을 변경하고 싶을 때 사용